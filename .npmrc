# Deterministic installs: no package lifecycle/build scripts are executed.
# (This sandbox forbids spawning child processes, which npm's rebuild step requires.)
ignore-scripts=true
fund=false
audit=false
